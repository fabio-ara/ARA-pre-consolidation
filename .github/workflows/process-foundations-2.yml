name: Process second educational foundations batch
on: [push]
permissions:
  contents: write
jobs:
  process:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: python .github/process-foundations-2.py
        env:
          GITHUB_TOKEN: ${{ github.token }}
      - run: |
          git config user.name "ARA consolidation control"
          git config user.email "actions@users.noreply.github.com"
          git rm .github/workflows/process-foundations-2.yml .github/process-foundations-2.py
          git add CONSOLIDATION_STATUS.yaml SOURCE_INVENTORY.jsonl
          git commit -m "chore: process second educational foundations batch"
          git push
