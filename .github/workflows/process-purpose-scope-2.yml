name: Process second purpose and scope batch
on: [push]
permissions:
  contents: write
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/process-purpose-scope-2.py
      - run: |
          git config user.name "ARA consolidation control"
          git config user.email "actions@users.noreply.github.com"
          git rm .github/workflows/process-purpose-scope-2.yml .github/process-purpose-scope-2.py
          git add CONSOLIDATION_STATUS.yaml SOURCE_INVENTORY.jsonl
          git commit -m "chore: process second purpose and scope batch"
          git push
